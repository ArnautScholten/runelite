language: java
sudo: false
dist: trusty
cache:
  directories:
    - $HOME/.m2
jdk:
- oraclejdk8

install: true
script: ./travis/build.sh
after_success:
  - bash <(curl -s https://codecov.io/bash)
notifications:
  irc: "irc.rizon.net#runelite"

    