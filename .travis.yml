language: java
sudo: false
dist: trusty
cache:
  directories:
    - $HOME/.m2
jdk:
- oraclejdk8

install: true
script: ./travis/build.sh
after_success:
  - ./travis/post-build.sh
notifications:
  irc: "irc.rizon.net#runelite"

    